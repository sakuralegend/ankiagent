# 🎯 VIỆC ĐANG LÀM

> Phiếu này bị GHI ĐÈ ở việc kế tiếp. Xong phiên: để TRỐNG hoặc chừa đúng MỘT đầu
> việc (cửa **S19**, QD-25). Việc chưa tới lượt ⇒ `SONO.md` kèm HẠN XOÁ.

## Thẻ CHI PHỐI — lô 1: 14 giới từ ≈ 85 thẻ, SOẠN MỚI như lô kho
Chốt 10/08: chỗ yếu nhất là **chi phối** (`в` đòi cách 4 hay 6?) — không suy được nên nó là TỪ VỰNG, chữa bằng thẻ nhớ chứ không bằng web bài tập (web điền-từ đã RÚT).
- 🔴 **1 thẻ = 1 CÔNG DỤNG mà chọn sai thì câu sai** — đếm RIÊNG từng từ, ra 3–7, CẤM chia đều. `к` 1 cách mà cần 6 thẻ, `че́рез` 1 cách chỉ 3 ⇒ số CÁCH không quyết định.
- Tính là công dụng riêng: đổi cách · khác miền nghĩa · phải-thuộc-riêng (`на рабо́те`) · cặp dễ lẫn · thành ngữ đông cứng · biến thể chính tả (`в→во`).
- 🔴 Ô `HuongDan` cũ chỉ là **ĐÁP ÁN** (công dụng nào có thật, cách mấy) — **CẤM chép văn**. Soạn mới hoàn toàn, user duyệt từng lô, đúng kỷ luật `data/huongdan/README.md`.
- Cần **chuẩn nội dung RIÊNG**: `v3` (chẻ từ · cách nhớ · họ hàng) là chuẩn thẻ TỪ VỰNG. Giữ 3 con số cứng của v3 (1 màn hình iPhone · ≤2 ô đỏ · không khối dùng chung).
- Cây deck `GRAMMAR::chi phối::giới từ::в — cách 4/6 — vào trong` + nhãn `cach::N` cắt ngang.
- Mặt trước cho sẵn 2 mảnh; mặt sau BẮT BUỘC có vế bị loại `в шко́лу` ✓ ↔ `в шко́ле` ✗. Cụm bắt chéo (`на/за/че́рез неде́лю`): MỘT thẻ đối chiếu ở deck khó nhất, nhãn cả ba.
- Full sync 1 lần (loại thẻ mới): DỪNG bảo user Upload (laptop) → Download (VPS). Ranh giới: 49 động từ + số/lượng để lô sau; tính từ KHÔNG làm (hợp dạng ≠ chi phối).

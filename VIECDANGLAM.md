# 🎯 VIỆC ĐANG LÀM

> Phiếu này bị GHI ĐÈ ở việc kế tiếp. Xong phiên: để TRỐNG hoặc chừa đúng MỘT đầu
> việc (cửa **S19** đếm mục `##`, QD-25). Việc chưa tới lượt ⇒ `SONO.md` kèm HẠN XOÁ.

## ⏳ Phiên 05/08 ĐANG CHẠY: k66(4) + k38(16) + k39(16) — chờ 3 agent, rồi nạp

Vào phiên **80% hạn mức**, user chốt **36 từ**, dự ăn ~61%. `moi --apply` đã chạy: 4 từ
mới thành **k66** ở đầu hàng đợi (**66 lô / 1043 từ**). Đóng phiên theo mục *"Khi một lô
báo xong"* ở `kho/TIEPTUC.md` — soat · dodai · dolo.tsv · xong · nap (Anki phải mở).

🔴 **k66 đắt bất thường, và đó là lựa chọn có ý thức — đừng lấy làm tiền lệ.** `congcu.py moi`
tự cảnh báo *"lô dưới 10 từ đắt gấp ~3 lần mỗi từ"*: 4 từ ngốn ~16% (≈4%/từ so với 1,4%/từ của
lô 16 từ). User vẫn chọn chạy vì luật **"từ mới ưu tiên hơn mọi thứ"** (28/07). ⇒ **Ngày chỉ có
~4 từ mới thì phương án rẻ hơn là ĐỂ DỒN** — `moi` gộp dồn sẵn. Lần sau **trình cả hai phương án
rồi để user chọn**, đừng mặc định chạy.

🆕 **k38/k39 cùng `places::city` chạy song song nên mù nhau** — đã dán danh sách từ của lô kia vào
cả hai lời nhắn kèm *"chỉ nêu nghĩa, đừng dựng hệ thống chung"*. Va chạm quét được: k66 **2** cụm
(`по́зже`·`че́рез` cùng đề bài "sau", cả hai TRONG lô) · k38 **5** · k39 **6** (`университет`·
`институт` cùng "trường đại học", cả hai TRONG lô). Hai lô chia phần qua báo cáo, **luồng chính
chuyển đề nghị chứ không làm trọng tài**.

📌 Cuối phiên ghi **điểm đo thứ ba** (%hạn mức thật / số từ) vào bảng 📊 CHI PHÍ ở `TIEPTUC.md` —
n=2 chưa đủ hạ tiếp 1,4%. **Lần đầu đo lô 4 từ**, số của k66 giữ riêng.

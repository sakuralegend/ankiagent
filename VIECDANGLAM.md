# 🎯 VIỆC ĐANG LÀM
> Phiếu này bị GHI ĐÈ ở việc kế tiếp. Xong việc thì xoá nội dung, để lại đúng dòng tiêu đề.

## ✅ PHIÊN 04/08 đợt 2 XONG — `k35`(15) · `k36`(15) · `k37`(17) = **47 từ, 3 lô**

Cả ba đã soạn → soát → nạp → commit. `nap` khớp tuyệt đối **47/47 note**. **43 lô / 662 từ duyệt /
377 chờ.** Lô kế tiếp: **k38**. Bài học phiên này nằm ở `data/huongdan/kho/TIEPTUC.md` mục đợt 2.

### Việc còn nợ, để phiên sau nhặt

1. ⚠️ **`компания` đụng `фирма`** (cùng `n` + FEM ♀ nên badge không cứu). Agent k36 chỉ sửa được
   phía `компания`; **tới lô của `фирма` thì nhớ tách nốt.**
2. 🔴 **`семья́` vẫn đụng chuỗi "gia đình" với `род` và `дома́шний`** — tới lô của hai từ kia thì tách.
3. ✅ **Đã vá `справа` TẬN GỐC** (user duyệt cuối phiên): `GrammarJSON` của note + `PoS` + huy hiệu
   giống + `tudien.json`. Đã kiểm trên thẻ thật: không còn dạng biến cách nào. **Không có "bộ đệm"
   nào để hỏng** — QD-11 đã bỏ file cache, thẻ là nơi duy nhất (tôi từng viết sai chỗ này, user bắt).
4. ✅ **Đã đo hạn mức lần hai: 47 từ ăn 65%** (đầu phiên 15% → cuối 80%) ⇒ **1,38%/từ**. Đã hạ mô
   hình 1,5 → **1,4%/từ** và nới trần phiên lên **~58 từ** ở `kho/TIEPTUC.md` mục 📊 CHI PHÍ.

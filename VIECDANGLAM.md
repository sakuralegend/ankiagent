# 🎯 VIỆC ĐANG LÀM

> Phiếu này bị GHI ĐÈ ở việc kế tiếp. Xong phiên: để TRỐNG hoặc chừa đúng MỘT đầu
> việc (cửa **S19**, QD-25). Việc chưa tới lượt ⇒ `SONO.md` kèm HẠN XOÁ.

## Thẻ CHI PHỐI — đợt này CHỈ 14 giới từ ≈ 85 thẻ. Hình dạng thẻ ĐÃ DUYỆT 10/08
Yếu nhất là **chi phối** (`в` đòi cách 4 hay 6?) — không suy được nên là TỪ VỰNG, chữa bằng thẻ nhớ chứ không bằng web bài tập (web điền-từ đã RÚT 10/08).
- **Trước**: các từ ở dạng gốc + 1 dòng Việt. **Giới từ LUÔN cho sẵn** (lô động từ thành 3 ô: `игра́ть`+`в`+`футбо́л`) ⇒ user chỉ CHIA, không CHỌN. **Sau**: đáp án · huy hiệu cách · dòng đối chiếu. KHÔNG giải thích, KHÔNG ô đỏ.
- 🔴 **Dòng đối chiếu phải MÁY SINH lúc nạp, CẤM viết tay** — soạn lô nhỏ thì thẻ làm trước thiếu mục của thẻ làm sau, mà không gì nhắc quay lại. Cùng cách `congcu.py nap` nối bảng chia. Gom theo **DANH TỪ** (cặp `на рабо́ту` ↔ `с рабо́ты` bắt chéo giới từ).
- 🔴 **Dòng tiếng Việt mơ hồ = hỏng IM LẶNG**: gõ đúng bị chấm sai → Again → học ngược. "ở trường" ứng 2 đáp án, phải viết "**đang ở trong** trường".
- 🔴 **1 thẻ = 1 CÔNG DỤNG mà chọn sai thì câu sai** — đếm RIÊNG từng từ, ra 3–7, CẤM chia đều (`к` 1 cách mà cần 6 thẻ). Gồm: đổi cách · khác miền nghĩa · phải-thuộc-riêng · thành ngữ · biến thể (`в→во`).
- Ô `HuongDan` cũ chỉ là **ĐÁP ÁN** — **CẤM chép văn**. Soạn mới hoàn toàn, user duyệt từng lô nhỏ.
- Deck `GRAMMAR::chi phối::giới từ::в — cách 4/6 — vào trong` + nhãn `cach::N`. Máy móc dựng sẵn cho cả 3 lô, nhưng đợt này chỉ NẠP giới từ; user ôn thật 1–2 tuần rồi mới tính lô sau.
- Cố ý BỎ NGỎ: bộ này không dạy cách CHỌN giới từ. Full sync 1 lần do loại thẻ mới: DỪNG bảo user Upload (laptop) → Download (VPS). Lô sau: 49 động từ (28 cần giới từ) + số/lượng; tính từ KHÔNG làm (hợp dạng ≠ chi phối).

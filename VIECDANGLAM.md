# 🎯 VIỆC ĐANG LÀM

> Phiếu này bị GHI ĐÈ ở việc kế tiếp. Xong phiên: để TRỐNG hoặc chừa đúng MỘT đầu
> việc (cửa **S19**, QD-25). Việc chưa tới lượt ⇒ `SONO.md` kèm HẠN XOÁ.

## Thẻ CHI PHỐI — lô 1: 14 giới từ ≈ 85 thẻ. HÌNH DẠNG THẺ ĐÃ DUYỆT 10/08
Chốt 10/08: chỗ yếu nhất là **chi phối** (`в` đòi cách 4 hay 6?) — không suy được nên nó là TỪ VỰNG, chữa bằng thẻ nhớ chứ không bằng web bài tập (web điền-từ đã RÚT).
- **Trước**: 2 từ nguyên thể (`в` + `шко́ла`) + 1 dòng Việt. **Sau**: đáp án · huy hiệu cách · dòng đối chiếu. KHÔNG giải thích, KHÔNG ô đỏ — user chốt "sai thì bấm Again, lịch ôn lo phần nhớ".
- 🔴 Dòng đối chiếu gom theo **DANH TỪ**, KHÔNG theo giới từ — cặp đáng giá nhất bắt chéo (`на рабо́ту` ↔ `с рабо́ты`). Mỗi mục: cụm + cách + nghĩa Việt.
- 🔴 **RỦI RO CHÍNH — dòng tiếng Việt mơ hồ là thẻ hỏng IM LẶNG**: gõ đúng bị chấm sai → Again → học ngược. "ở trường" ứng 2 đáp án, phải viết "**đang ở trong** trường". Cùng luật deck 1-gõ đã có.
- 🔴 **1 thẻ = 1 CÔNG DỤNG mà chọn sai thì câu sai** — đếm RIÊNG từng từ, ra 3–7, CẤM chia đều (`к` 1 cách mà cần 6 thẻ). Tính là công dụng riêng: đổi cách · khác miền nghĩa · phải-thuộc-riêng · thành ngữ · biến thể (`в→во`).
- Ô `HuongDan` cũ chỉ là **ĐÁP ÁN** — **CẤM chép văn**. Soạn mới hoàn toàn, user duyệt từng lô.
- Cây deck `GRAMMAR::chi phối::giới từ::в — cách 4/6 — vào trong` + nhãn `cach::N` cắt ngang.
- Cố ý BỎ NGỎ: mặt trước luôn cho sẵn giới từ ⇒ bộ này KHÔNG dạy cách CHỌN giới từ (hoãn có chủ ý, không phải sót). Full sync 1 lần do loại thẻ mới: DỪNG bảo user Upload (laptop) → Download (VPS). Lô sau: 49 động từ + số/lượng; tính từ KHÔNG làm (hợp dạng ≠ chi phối).

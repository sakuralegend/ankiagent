# Secrets - TUYỆT ĐỐI không đưa lên git
.env

# Python
__pycache__/
*.pyc
venv/
.venv/

# Dữ liệu Anki của container trên VPS (nếu lỡ chạy compose ngay trong repo)
anki-data/

# Cấu hình cục bộ của Claude Code
.claude/

# Tạp
*.log
.DS_Store
Thumbs.db
suadeck_resume.json
last_deck.json

# Bản sao lưu collection (mỗi bản ~36MB) — dữ liệu, không phải mã nguồn
backups/

# Dump từ điển gốc (~8MB) — tải lại bằng: python -m grammar_forms.irregular_plurals
data/nouns.csv
data/openrussian_cache.json
